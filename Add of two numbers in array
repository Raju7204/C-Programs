#include <stdio.h>

int main()
{
   int n,i,j,a[30],k;
   scanf("%d",&n);
   for(i=0;i<n;i++)
   {
       scanf("%d",&a[i]);
   }
   scanf("%d",&k);
   for(i=0;i<n;i++)
   {
       for(j=i+1;j<n;j++)
       {
            if(a[i]+a[j]==k)
            printf("[%d , %d]",a[i],a[j]);
       }
    }

    return 0;
}
